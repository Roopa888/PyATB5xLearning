squares=[1,2,4,16,64,128]
print(squares)
squares.pop() # removes the last element if no index is given .if index present it will remove the element at index
print(squares)
squares.pop(3)
print(squares[3])
print(squares)